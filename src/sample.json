{
  "question": "Implement a `DebouncedInput` component in React. This component should accept a `value`, an `onChange` handler, and a `delay` prop. The `onChange` handler should only be called after the user has stopped typing for the specified `delay` (in milliseconds). Consider edge cases like rapid input changes and component unmounting during the debounce period. Ensure the component is accessible.",
  "initialCode": "import React, { useState, useEffect, useRef } from 'react';\n\nconst DebouncedInput = ({ value, onChange, delay = 500, ...props }) => {\n  const [internalValue, setInternalValue] = useState(value);\n  const timeoutRef = useRef(null);\n\n  useEffect(() => {\n    // Update internal value if the external value changes\n    if (value !== internalValue) {\n      setInternalValue(value);\n    }\n  }, [value]);\n\n  const handleChange = (event) => {\n    setInternalValue(event.target.value);\n\n    // Clear any existing timeout\n    if (timeoutRef.current) {\n      clearTimeout(timeoutRef.current);\n    }\n\n    // Set a new timeout\n    timeoutRef.current = setTimeout(() => {\n      onChange(event.target.value);\n    }, delay);\n  };\n\n  // Cleanup the timeout on unmount\n  useEffect(() => {\n    return () => {\n      if (timeoutRef.current) {\n        clearTimeout(timeoutRef.current);\n      }\n    };\n  }, []);\n\n  return (\n    <input\n      type=\"text\"\n      value={internalValue}\n      onChange={handleChange}\n      aria-label=\"Debounced Input\"\n      {...props}\n    />\n  );\n};\n\nexport default DebouncedInput;",
  "type": "coding",
  "error": null
}